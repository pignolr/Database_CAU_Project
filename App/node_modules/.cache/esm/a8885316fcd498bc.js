let Router;_fcd‍.w("express",[["Router",["Router"],function(v){Router=v}]]);

module.exports = Router({mergeParams: true})
    .get('/logout', async(req, res, next) => {
        // Destroy cookie
        cookies.set('username', {expires: new Date(1)});
        res.redirect('/');
        next();
    })
    .post('/logout', async(req, res, next) => {
        // Destroy cookie
        cookies.set('username', {expires: new Date(1)});
        res.redirect('/');
        next();
    })
